import { StyleSheet } from "react-native";

export default () => StyleSheet.create({
  container: {
    borderRadius: 40,
    backgroundColor: '#F00'
  },
  inputText: {
    backgroundColor: '#aee6ca',
    borderRadius: 30,
  }
})
